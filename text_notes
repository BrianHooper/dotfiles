Linux system
    vA && rA                       # Edit and reload .bashrc     
    ps -ax                         # List all processes          
    awk '{print $4,$5}'            # Print columns 4 and 5
    fg                             # Return to background process
    fd                             # cd to flash drive
    ds                             # cd to downloads and list contents
Programs
    dpkg -i file                   # Install using package manager
    apts / aptg                    # Shorthand for apt install & apt search
    lsblk                          # List block devies
Scripts & functions
    flashback                      # Backup /media/brian/Hoopflash
    run                            # Start a program outside of console
    copy source dest               # Copy files/directories with progress
    scrtv / notv                   # Switch screen profile
Compressed Files
    extract                        # Uncompress .zip, .tar.gz, .tar.bz2
    tar xvzf file.tar.gz           # Uncompress .tar.gz
    tar xvjf file.tar.bz2          # Uncompress .tar.bz2
